import React from "react";

const NoContentTitle = ({title}) => {
    return(
        <h1 className="text-center text-5xl font-bold text-light-gray">{title}</h1>
    );
}

export default NoContentTitle;